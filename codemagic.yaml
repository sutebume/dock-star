workflows:
  ios-release:
    name: Dock Star iOS Release
    max_build_duration: 120
    instance_type: mac_mini_m2
    integrations:
      app_store_connect: Codemagic
    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.dockstar.game
      groups:
        - apple_credentials
        # Holds ALLOW_TEST_ADS. Set it to "true" in the Codemagic UI to build
        # with Google placeholder ads (visible on TestFlight); set it to
        # "false" for a release build, which then forces TEST_ADS=false in
        # js/ads.js. See scripts/check-ads-mode.js.
        - ads_testing
      vars:
        BUNDLE_ID: "com.dockstar.game"
        XCODE_SCHEME: "App"
        XCODE_PROJECT: "ios/App/App.xcodeproj"
      node: 22.13.0
      xcode: latest
    scripts:
      - name: Install npm dependencies and Capacitor iOS
        script: |
          npm install
          npm install @capacitor/ios
      - name: Check ad mode (blocks accidental test-ads release)
        script: |
          node scripts/check-ads-mode.js
      - name: Add and sync iOS platform
        script: |
          npm run build:www
          npx cap add ios
          npx cap sync ios
      - name: Configure Info.plist
        script: |
          plutil -insert ITSAppUsesNonExemptEncryption -bool false ios/App/App/Info.plist
          plutil -insert GADApplicationIdentifier -string "ca-app-pub-9105653107748286~1268068332" ios/App/App/Info.plist
          plutil -replace CFBundleVersion -string "$BUILD_NUMBER" ios/App/App/Info.plist
      - name: Generate iOS app icon
        script: |
          npm install @capacitor/assets
          mkdir -p resources
          # App Store icons must be exactly 1024x1024 with NO alpha channel.
          # Source is 1081x1024, so centre-crop to square, then flatten alpha
          # via a JPEG round-trip before converting back to PNG.
          sips -c 1024 1024 dock-star-2-ios-icon.png --out resources/icon-square.png
          sips -s format jpeg -s formatOptions best resources/icon-square.png --out resources/icon-flat.jpg
          sips -s format png resources/icon-flat.jpg --out resources/icon.png
          rm -f resources/icon-square.png resources/icon-flat.jpg
          npx @capacitor/assets generate --ios
      - name: Set up code signing
        script: xcode-project use-profiles
      - name: Build iOS IPA
        script: |
          xcode-project build-ipa \
            --project "$XCODE_PROJECT" \
            --scheme "$XCODE_SCHEME"
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
